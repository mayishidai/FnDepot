# 应用图标

放置应用图标 `agnes-ai-studio.png` 于此目录。

## 要求（来自 FnDepot 规则）
- 格式：PNG / WebP
- 大小：建议 512×512，单张 < 500KB
- 文件名需与 `apps/agnes-ai-studio.json` 中 `icon_url` 引用一致（`agnes-ai-studio.png`）

> 当前 `fnpack.json` / `apps/agnes-ai-studio.json` 中的 `icon_url` 为占位地址，
> 放入真实图标并确认可公网访问后，再改为实际 raw 链接。
